 encapsulation: keeping things related to a file or program only in one file or program, modularity, self contained

going over the cut problem from proj2, things like the any() method, join() method, 
